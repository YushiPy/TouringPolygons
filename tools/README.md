# Tools

Repository-level helper scripts live here.

Prefer package-local scripts when a command only applies to one solver or app. Use this directory for cross-package maintenance, migration, or reporting helpers.
