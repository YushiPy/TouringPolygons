--- a/tspn_core/soc.h
+++ b/tspn_core/soc.h
@@ -13,6 +13,7 @@
 #include "tspn_core/common.h"
 #include <atomic>
 #include <cstdint>
+#include <limits>
 #include <string>
 #include <vector>
 
@@ -52,8 +53,12 @@
    */
   void update_cutoff(double new_cutoff);
 
+  /** Limit subsequent specialized-oracle calls to the remaining solve time. */
+  void set_time_limit(double seconds);
+
   [[nodiscard]] std::uint64_t get_num_calls() const;
   [[nodiscard]] std::uint64_t get_tpp_socp_fallback_calls() const;
+  [[nodiscard]] std::uint64_t get_tpp_time_limit_calls() const;
   [[nodiscard]] std::uint64_t get_total_solve_nanoseconds() const;
   [[nodiscard]] std::uint64_t get_optimize_nanoseconds() const;
 
@@ -65,6 +70,9 @@
   double cutoff_value = 1e100;
   mutable std::atomic<std::uint64_t> num_calls{0};
   mutable std::atomic<std::uint64_t> tpp_socp_fallback_calls{0};
+  mutable std::atomic<std::uint64_t> tpp_time_limit_calls{0};
+  std::atomic<std::int64_t> deadline_nanoseconds{
+      std::numeric_limits<std::int64_t>::max()};
   mutable std::atomic<std::uint64_t> total_solve_nanoseconds{0};
   mutable std::atomic<std::uint64_t> optimize_nanoseconds{0};
 };
--- a/tspn_core/soc.cpp
+++ b/tspn_core/soc.cpp
@@ -180,6 +180,21 @@
   if (use_cutoff && new_cutoff < cutoff_value) {
     cutoff_value = new_cutoff;
   }
+}
+
+void SocSolver::set_time_limit(double seconds) {
+  using Clock = std::chrono::steady_clock;
+  const auto now = std::chrono::duration_cast<std::chrono::nanoseconds>(
+                       Clock::now().time_since_epoch())
+                       .count();
+  const auto allowance = std::chrono::duration_cast<std::chrono::nanoseconds>(
+                             std::chrono::duration<double>(std::max(0.0, seconds)))
+                             .count();
+  deadline_nanoseconds.store(
+      allowance >= std::numeric_limits<std::int64_t>::max() - now
+          ? std::numeric_limits<std::int64_t>::max()
+          : now + allowance,
+      std::memory_order_relaxed);
 }
 
 std::uint64_t SocSolver::get_num_calls() const { return num_calls.load(); }
@@ -188,6 +203,10 @@
   return tpp_socp_fallback_calls.load();
 }
 
+std::uint64_t SocSolver::get_tpp_time_limit_calls() const {
+  return tpp_time_limit_calls.load();
+}
+
 std::uint64_t SocSolver::get_total_solve_nanoseconds() const {
   return total_solve_nanoseconds.load();
 }
@@ -296,9 +315,18 @@
     const double cutoff = use_cutoff ? cutoff_value
                                      : std::numeric_limits<double>::infinity();
     try {
+      const auto now = std::chrono::duration_cast<std::chrono::nanoseconds>(
+                           Clock::now().time_since_epoch())
+                           .count();
+      const double remaining_seconds = std::max(
+          0.0, static_cast<double>(deadline_nanoseconds.load(
+                   std::memory_order_relaxed) - now) /
+                   1e9);
       const auto result = tpp::tpp_convex_solve_certified(
           to_tpp_point(*start), to_tpp_point(*target), regions, workspace,
-          tolerance, cutoff);
+          tolerance, cutoff, remaining_seconds);
+      tpp_time_limit_calls.fetch_add(result.time_limited,
+                                     std::memory_order_relaxed);
       std::vector<Point> points;
       points.reserve(result.path.size());
       for (const auto point : result.path) {
--- a/python/tspn_bnb2/core/_tspn_bindings.cpp
+++ b/python/tspn_bnb2/core/_tspn_bindings.cpp
@@ -189,16 +189,19 @@
   if (initial_solution != nullptr) {
     baba.add_upper_bound(*initial_solution);
   }
+  soc.set_time_limit(timelimit);
   baba.optimize(timelimit, eps);
 
   auto statistics = baba.get_statistics();
   const auto soc_calls = soc.get_num_calls();
   const auto tpp_socp_fallback_calls = soc.get_tpp_socp_fallback_calls();
+  const auto tpp_time_limit_calls = soc.get_tpp_time_limit_calls();
   const auto soc_total_ns = soc.get_total_solve_nanoseconds();
   const auto soc_optimize_ns = soc.get_optimize_nanoseconds();
   statistics["soc_num_calls"] = std::to_string(soc_calls);
   statistics["tpp_socp_fallback_calls"] =
       std::to_string(tpp_socp_fallback_calls);
+  statistics["tpp_time_limit_calls"] = std::to_string(tpp_time_limit_calls);
   statistics["oracle_backend"] = oracle_backend;
   statistics["oracle_tolerance"] = std::to_string(oracle_tolerance);
   statistics["soc_total_seconds"] =
